# installation

- put `tech.ticktech.mouse.json` to chrome conf dir: `$CHROME_DIR/NativeMessagingHosts/`
- change `path` to the real abs path of `mouse.sh`.
- change `allowed_origins` to the extension id of this extension.
